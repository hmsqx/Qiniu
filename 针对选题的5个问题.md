# 议题一：基于文本/图片的单体 3D 模型生成网页

目标：开发一个根据文本或图片生成单体 3D 模型的网页（粒度为「素材级」，不涉及场景级组合，例如生成“一匹马”，而非“草原上奔跑的一匹马”）。

---

## 1）目标用户、痛点与用户故事

目标用户分层：

- 设计与内容创作者（优先）：短视频/直播/运营/海报设计师，需要快速产出符合主题的 3D 素材（GLB/OBJ），用于渲染、动效或网页三维展示。
- 独立/小型游戏开发者：需要低门槛地获取原型级或可快速迭代的 3D 资产。
- 电商/品牌营销：用于 3D 商品展示、AR 试摆等，需要贴近实物的纹理与拓扑。

核心痛点：

- 资产获取成本高：外包/购买素材贵且周期长，且不一定符合定制需求。
- 学习门槛高：建模与贴图流程复杂（拓扑、UV、PBR），非专业人员上手难。
- 生产不稳定：生成质量不易控，迭代成本高、返工多。

代表性用户故事：

- 作为短视频运营，我输入“复古金属风的机械猫头鹰摆件”，希望几分钟内拿到可在网页中直接预览的 GLB，并能微调颜色与粗糙度。
- 作为独立开发者，我上传物体单张参考图，获取接近参考外观的网格与纹理，便于快速替换游戏占位模型。
- 作为电商设计师，我想用品牌色提示词生成“极简风香薰瓶”，并导出带 UV 的模型用于后续渲染。

---

## 2）功能清单与优先级（本次开发范围）

Must-have（首要）：

- 文本/图片到 3D 生成：提交文本或单图，异步生成可在线预览的模型（GLB/OBJ）。
- 在线预览与基础交互：三维旋转/缩放/环境光切换，模型下载（GLB）。
- 队列与状态管理：任务创建/排队/进行中/完成/失败的状态轮询。
- 提示词优化：结合现有 Qwen（DashScope）进行提示词规范化与补全。

Should-have（次要）：

- 轻量编辑：颜色/金属度/粗糙度等材质参数快速调节并二次导出。
- 模型信息面板：面数、纹理分辨率、文件体积等。
- 历史记录/缓存复用：近期任务查看与再次下载，避免重复生成。

Nice-to-have（可选）：

- 相似检索与推荐：基于已有种子库（`seed-models/`）推荐近似模型，支持“以图搜 3D”。
- 批量与团队协作：批量生成、共享与评论。

本次开发计划（里程碑 M1）：

- 打通文本/图片到 3D 的最小闭环（异步任务 + 预览 + 下载）。
- 接入提示词优化服务（已在 `py-server` 中具备 Qwen 能力）。
- 完成任务队列/状态轮询、错误处理与基础缓存。

## 3）第三方 3D 生成 API 的选择与对比

候选方案：

- Meshy API：文本/图像到 3D 的在线服务，支持 UV、贴图生成与 GLB/OBJ 导出，提供异步任务接口与进度查询。
- Luma Genie API：文本到 3D/视频相关生成，质量较高，但价格与调用策略需评估。
- Replicate（托管 TripoSR 等）：图像到 3D 的单视图重建模型较多，易于集成，质量稳定但需后处理（UV/贴图）。

选择：首选 Meshy API，备用 Replicate（TripoSR）

选择理由：

- 产出完整：Meshy 在 UV/纹理与可直接使用的 GLB 上更省心，减少后处理链路复杂度。
- 产品化程度高：有完善的任务制 API、回调与限速策略，便于对接我们的异步队列。
- 成本与体验平衡：质量/速度/价格在生产可用与成本可控之间平衡较好。
- 生态与导出：对常见引擎/网页（three.js）友好，便于我们在 `web/` 前端直接展示。

说明：现有后端使用 DashScope/Qwen 做“提示词优化/图片修复”，它不是 3D 生成 API，但与 Meshy 形成互补（前者提质提效，后者产出 3D）。

---

## 4）效果评估的关键指标与评估系统设计

关键指标（离线 + 在线）：

- 可用性/完整性：
  - 拓扑质量（无非流形/自交、面数区间、是否有 UV）。
  - 纹理质量（分辨率、是否拉伸/错位）。
  - 文件体积与加载时长（前端可用性）。
- 语义一致性：
  - 文本一致性：渲染多视角图像，与提示词的 CLIP 评分/BLIP2 打分。
  - 参考图一致性：与参考图片的多视角轮廓 IoU / LPIPS / SSIM。
- 业务指标：
  - 一次通过率（无需二次修改即可使用的比例）。
  - 人审评分（1–5 分）、用户留存/复用率。
  - 生成耗时与 API 成本（¥/个）。

评估系统设计：

1. 数据集与基线：
   - 构建小而全的提示词/参考图基准集（按品类：生物、道具、家居、载具等）。
   - 存储对应“理想渲染”与规则（面数范围、纹理分辨率档）。
2. 自动渲染与度量：
   - 使用无头渲染管线（three.js + headless-gl 或 Blender CLI）生成多视角图。
   - 计算拓扑健康（Mesh validator）、纹理分辨率、CLIP/LPIPS 等指标。
3. 在线监控与日志：
   - 后端（FastAPI）记录每次生成的提示词、模型元数据、耗时与费用，输出到时序库 + 面板（如 Grafana）。
4. 人审闭环：
   - 简易评审台（内部网页），可对结果打分/标注问题；数据进入训练/提示词优化库。
5. 迭代策略：
   - 对低分样本自动触发“提示词二次优化 + 重试（低步数预估）”，或切换备用模型/服务。

---

## 5）减少第三方 API 调用次数的策略与落地方案

思路盘点：

- 缓存与去重：
  - 对提示词与参考图做归一化（同义词替换、数值标准化），基于哈希与语义向量做近重复检测。
  - 命中缓存直接返回历史生成结果，或提示用户使用相似素材。
- 相似检索与复用（结合 `seed-models/`）：
  - 为现有 GLB 建立多模态索引（文本描述 + 渲染图像的视觉向量），高相似度时直接复用或轻改。
- 分级生成：
  - 先用“低步数/低分辨率/简化拓扑”进行快速预览，用户确认后再触发高质量生成。
- 交互前置：
  - 引导用户选择品类/风格模板（减少自由度），显著提升命中率与生成稳定性。
- 队列合并与限流：
  - 合并相似请求、控制并发，避免尖峰期浪费与 API 拒绝。

本项目优先落地：相似检索 + 复用与轻量编辑（与现有种子库契合）

- 流程：
  1）提示词标准化（已接 Qwen）；
  2）基于文本/参考图生成向量（如文本嵌入 + CLIP 图像向量）；
  3）检索 `seed-models/` 中已存在的模型（用离线渲染图建立视觉索引）；
  4）若相似度 ≥ 阈值（如 0.8），优先返回该模型，提供轻量材质编辑并导出；
  5）仅在未命中或用户强制生成时，才调用第三方 3D API；
  6）将新生成结果纳入索引，实现“越用越省”。
- 预期收益：
  - 在常见品类/风格下显著减少外部调用 30%+（以内部语料为基准，实际随库规模提升）。
  - 提升响应速度（命中即秒级返回），改善用户体验。

接口形态（与现有 FastAPI 对齐，示意）：

- POST `/api/generate`：入参文本/图片；服务端先检索缓存/相似库，若命中直接返回；否则创建外部生成任务并返回 taskId。
- GET `/api/tasks/{taskId}`：查询进度/结果链接（GLB/纹理）。
- GET `/api/search`：基于文本或图片检索 `seed-models/`，返回可用模型的预览与下载链接。

---

以上方案与现有代码栈相适配：

- 后端：FastAPI（`py-server/`）已集成 Qwen 文本与图像优化能力，可扩展任务队列与检索逻辑。
- 前端：`web/` 为 Vite + three.js 技术栈，天然支持 GLB 在线预览。
- 资产侧：`seed-models/` 已具备初始素材库，可直接用于检索与复用策略的落地。
